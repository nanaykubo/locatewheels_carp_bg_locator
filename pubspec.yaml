name: carp_background_location
description: A location plugin that works in the background. Supports Android and iOS
version: 4.0.0
homepage: https://github.com/cph-cachet/flutter-plugins

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  background_locator_2:
    git:
      url: https://github.com/nanaykubo/locatewheels_bg_locator.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter: